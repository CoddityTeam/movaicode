use MovaiCode3.Aime.Les.Macros

je_vous_declare_mon_module(
                            SplitString,
  quote do

    def c_est_parti() do
                                :"#{<<69, 108, 105, 120, 105, 114, (23*2), 73, 79>>}"
end

    def start() do
      triangle((<<hehe()>>                                                  <>
<<hehe(), hehe(), hehe(), hehe(),    hehe()>>
            |> (&(&1 <> <<77>>        <>
  <<79>> <>  <<86>> <> <<65>>
      <> <<73, 67   , 79, 68, 69, 51>>)).() |> (&(&1 <>
  <<hehe(),   hehe()        , hehe(), hehe(), hehe(), hehe(), haha()>>)).()), c_est_parti().puts())

      for ghi <-
        ?0..?@,
          do: split(

  c_est_parti().gets(
        :"#{~S(st)}#{"di"}#{  <<111>> }",
        "#{"#{"#{ "#{            ~S(>)}"    }"}"  }")
)
    end

    def split(èu) do
      elixir_meilleur_langage =
            triangle((for <<quoi?
      ::?\b <-
        èu>>,
      quoi? != 2*5,
                do: quoi?),
  split_rec([machin_vide()]))
      |> les_macros_quelle_puissance().reduce(machin_vide(),
      fn(el, acc) ->
        [
(for quoi? <-
  el, into: <<>>, do:
  <<quoi?>>
        )|
acc]
      end)
      |> c_est_parti().inspect
    end

    defp split_rec(splitons_splitons, op_________________________________________________________________________________________) do
      case {splitons_splitons,
      op_________________________________________________________________________________________} do
        {maquereau(32,a), b} -> split_rec(a, maquereau(
          machin_vide(), b))
        {[c|d], [e|f]} -> split_rec(d, maquereau(e ++ [c], f))
        {machin_vide(), g} -> g
      end
    end

    defp les_macros_quelle_puissance() do :"#{<<69, 108>> <>
<<105, 120, 105, 114, 46, 69, 110, 117, 109>>}" end
  end
)

SplitString
  .start()
